set(DLL_DIRS "@DLL_DIRS@")

get_filename_component(APP_DIR "${APP_PATH}" DIRECTORY)
if(APPLE)
    get_filename_component(BUNDLE_DIR "${APP_DIR}/../.." ABSOLUTE)
    set(APP_DIR "${BUNDLE_DIR}")
endif()

include(BundleUtilities)
fixup_bundle(${APP_PATH} "" "${DLL_DIRS}")

